name: 'Blossom Action'
description: 'Run blossom ci related task '
inputs:
  args:
    description: 'task argument'
    default: '{}'
branding:
  icon: 'activity'  
  color: 'green'
runs:
  using: "composite"
  steps: 
    - run: chmod +x ${{ github.action_path }}/script.sh
      shell: bash
    - run: ${{ github.action_path }}/script.sh ${{ inputs.args }}
      shell: bash
